# OrCAD 单导线真实录制验证

实机：VM2，OrCAD X Capture 24.1 P001，英语，VM 时钟 2026-09-17。本机时区的记录日期可能仍为 2026-09-16。

## 结论与边界

一份单导线样本通过真实录制、真实模型 API、受控 Tcl 编译、隔离应用执行、独立端点对照及同进程保存/关闭项目/重新打开。未手改模型响应或生成脚本。不是完整电路、器件、连接关系、仿真、复杂操作或其他版本的验证。

采集器界面只负责录制；分析由单独 CLI 发起。UI Map 仍为部分实机观察，不是完整菜单扫描。生成器目前只支持不相交横竖导线和唯一端点网标；网标仅有此前合成脚本实机测试，本真实样本不含网标。

## 输入与费用审计

- 录制 `20260916-183127-2af750`，10 条原始事件、14 个原图文件、去重后 8 张，8 条相关输入；仅排除两条没有截图的鼠标移动。
- 原始事件、截图、请求、响应和密钥不提交。采集未读取 OrCAD 对象数据，UI Automation 关闭。
- 请求配置模型沿用 `gpt-5.6`，返回模型为 `gpt-5.6-sol`；截图 detail 从 low 改为至少 high，便于识别原图的小字号状态栏坐标。
- 用户明确允许本录制 8 图及事件发至 OpenAI，追加最多 2 次，总上限 4。实际账本停在 3 次：第 1 次连接失败，usage 未知；第 2 次返回不完整计划；第 3 次识别成功。未花费第 4 次。
- 第 2 次 usage：7384 input、614 output、7998 total。第 3 次：28260 input、851 output、29111 total，其中 reasoning 207 已包含在 output 中；不能再次相加。两次已知响应合计 37109 tokens。没有用载荷字节数猜测美元费用，也不能说首次失败零费用。
- 源图对象读回只供独立验收，从未送入模型请求。

## 识别及通用逻辑修复

第 3 次响应确认 `PlaceWire(2,3,4,3)`，单位为页面英寸；无 unresolved 或 pending，末态选中了已生成的 `wire-1`。旧校验器把任何非空选择当作未完成操作，因此误拒绝。

修复允许合法、唯一的已创建对象作为被动末态选择；未知/重复 ID、未完成命令仍拒绝。新增 `--compile-existing` 对原始保存的模型结果做离线重编译：核对原请求、模型完整状态、解析内容、录制、上传图片哈希、指纹和账本，拒绝不一致输入；不读 API 密钥，不新增请求，不覆盖历史失败记录。哈希用于来源审计，不是抵御协调篡改的签名。

## 执行与独立对照

隔离工程：`RecorderModelReplay_20260917_0401`，PAGE1；不是源工程。

- 执行前：`BLANK_WIRES {}`、`SOURCE_UNCHANGED 1`。
- 脚本 SHA256：`e376b0139af64be82ee34254ef837c8484f00d27299a9fc20ab9e1358014f1b0`。
- 文件传输检查：4871 字节，CRC32 1674608710，本地与 VM 一致。
- Capture Command Window 用 `source` 加载同一生成文件，返回 `MODEL_REPLAY_CODE 0 MESSAGE {}`，所有生成断言通过，画布出现水平导线。
- 独立数据库读回：源和回放均为 `{{17 200 300 400 300}}`；按端点而不是相同的数据库 ID 判断匹配，`GEOMETRY_MATCH 1`。导线 1，网标 0；`SOURCE_UNCHANGED 1`。
- 页面 `GetPhysicalGranularity` 与 `GetDocUnitsPerInch` 均为 100；内部端点对应页面 `(2,3)→(4,3)`，不从屏幕像素猜尺寸。
- GUI 保存后关闭该项目，再从最近项目打开。重新激活页面并取得新 page handle，读回 `REOPENED {{17 200 300 400 300}} ALIASES {}`。
- 重复执行返回 `RERUN_CODE 1 MESSAGE {Wire count mismatch} AFTER {{17 200 300 400 300}}`，没有新增导线。

重开后仅聚焦项目管理器会让 `GetActivePage` 报 `Command Requires SchematicView`；点击已打开原理图画布，再聚焦命令窗口后读回成功。这是应用激活前提，已写入使用说明，不通过填假数据绕过。

孤立导线会出现少于两个连接的电气警告，这是本样本没有连接器件的预期状态，不能称为通过电路设计或仿真检查。没有重启 Capture，所以不声称独立进程重开验证。

## 本地复核

五个定向离线测试文件 **48/48 通过**，覆盖采集器 profile、证据配对、预算与失败账本、来源核验、末态选择、编译保护及知识包边界；包括 10 项本地模拟 HTTP 测试，不增加 OpenAI 请求。界面截图证据位于本次会话工具输出，不声称本文件包含独立截图附件。

提交前另将暂存文件导出到不含本地配置和密钥的独立目录复核，发现测试曾依赖忽略的 `config.json`。测试现改用各自临时目录中的合成配置；独立目录复测 **48/48 通过**，OrCAD 录制器构建成功。此次修复只影响测试，不修改模型响应、回放文件或调用次数。

首次推送因远程已有新提交而被拒绝。随后在独立工作副本合并远程 `b8947ff`，保留双方忽略规则和各应用条件编译，未改动原工作目录的其他未提交工作。合并后的 9 个定向测试文件 **155/155 通过**（含 OrCAD、共享客户端、JMP 和 Vivado）；OrCAD、JMP、Vivado、Quartus、默认 AutoCAD、3ds Max 六种采集器构建均成功，没有启动新构建的程序。这是合并回归，不是对其他应用重新做了一轮实机录制验证。
