# Stata/MP 18 real-recording validation — bounded PASS

Scope: old VM only; Stata/MP 18.0 English. The new VM was not operated. This document does not claim arbitrary Stata workflow coverage.

## Source truth acquired before API analysis

- Genuine continuous recording: `20260918-034557-105f30`, 89 events and 120 screenshot files, frozen PID 4832/session 2, no target loss or capture pause.
- Source workflow: blank default dataset → GUI Data Editor entry → standard Summary statistics → one-X linear regression → Save → Open → Browse verification.
- Data: `var1=[3,6,10,14]`, `var2=[5,10,14,22]`; both native `float`, `%9.0g`, four observations. No import, external source dataset or post-analysis edits.
- Summary means 8.25 and 12.75; displayed sample standard deviations 4.787136 and 7.182154.
- `regress var2 var1`: displayed slope 1.487273, intercept .48, R² .9827, adjusted R² .9741, root MSE 1.1568, N=4, F(1,2)=113.64.
- Native source saved to a new `Stata18-source-20260918-0346.dta` in VM Documents. Save success was observed, followed by successful Open of the same file. Browse then independently confirmed every cell and both variable storage types.
- Independent truth is retained in ignored local `dist/stata18-source-truth-20260918.json`, outside the recording and excluded from VLM input. Comparison requires exact cells/types/order and statistics within displayed rounding precision.

The earlier data-entry-only checkpoint remains separate and was not stitched into this recording. Startup Results text was cleared with explicit action-time user confirmation **before** this recording. The first recorded frame shows a blank History, Results and dataset; reviewed data and report frames contain the synthetic sample rather than licensing identifiers.

## Gates

| Gate | Current result |
| --- | --- |
| Source UI execution / source save-reopen | Passed for this bounded sample |
| Recording transfer / raw evidence inventory | 89 events, 120 source screenshot files copied locally |
| Analysis preflight | Passed after conservative product-version normalization; raw `521.18.0.120` preserved, visible software `18.0` |
| Paid Stata VLM analysis | 9/9 chunks passed after generic contract repairs; original provider responses preserved |
| Controlled `.do` generation | Single native file generated by the local renderer; no manual script repair |
| Unchanged native `.do` replay | Passed in a separate initially blank Stata/MP 18 instance, through File > Do |
| Independent source/replay equivalence | Passed: all eight cells, column order/types/formats, summary and regression, saved/reopened dataset |
| Offline regression | 39 Stata-related tests and 116 joint JMP/Stata tests passed |

Paid rounds are limited to two requests each, with completed chunks retained. Validation uses the configured `gpt-5.6`, low reasoning/verbosity, with high image detail explicitly selected in an ignored local validation config for small cell labels. Raw recording, source truth, screenshots, requests and responses remain local/ignored. Neither provider self-report nor generated-script assertions alone establish independent equivalence.

Preparation retained all 89 input events and all 120 referenced source images. Exact-byte deduplication produced 95 unique images; two boundary images are shared across chunks, for 97 request image slots over nine chunks. No events were excluded and no late-frame attribution warnings were emitted. Chunk event ranges are 1–12, 13–22, 23–32, 33–42, 43–54, 55–64, 65–75, 76–83 and 84–89. After the initial pre-execution security rejection, the user explicitly confirmed the recording, OpenAI destination and multiple bounded rounds. The first actual paid request then completed.

## First model response and contract repair

Run `a961fb19c0a17dbbca51f425`, chunk 1: the model identified the observed blank precondition correctly, but local validation rejected its epoch-millisecond timestamp under a numeric-cell bound. The schema also had not encoded the validator's fixed lifecycle binding names, and the validator treated a blank precondition as a mutating operation when checking navigation evidence overlap.

The generic contract was corrected: separate safe-integer timestamp validation, schema/prompt constants for dataset lifecycle bindings, explicit continuation/pending instructions, and a narrow observational-blank exception that still rejects any overlap with a mutating operation. The original paid response and its incorrect binding names were not edited or relabeled as passing. All 35 related offline tests pass after these changes, including epoch timestamps, mutating overlap rejection, cross-chunk time order and fixed binding schema tests.

Confirmed usage for the first failed attempt: 37,421 input tokens (0 cached), 663 output tokens (311 reasoning included), 38,084 total. The provider reported model `gpt-5.6-sol` for configured alias `gpt-5.6`; no dollar estimate is asserted. Run `de5de56c1e8493080c650394` retained the same source/chunks/images, but still failed the overly strict blank-evidence identity check; that check was then narrowly corrected without weakening mutation overlap rejection.

## Continuation repairs and paid audit

The next generic failure was persisted state: unobserved cells were `undefined` in memory but became `null` on disk, so a valid partial-column checkpoint failed equality on resume. Unknown cells now use explicit `null`; they are never filled with zero. A later model response repeated a SAVE already completed in the prior chunk because the continuation catalog exposed an output binding without explicit completion state. A completed `savedDataset` lifecycle entry now carries its source events and reopened state. Duplicate saves remain rejected rather than silently removed.

Explicit `--resume-from-run` reuse now verifies original source identity, full provider-facing payload/instructions/schema/settings, image hashes and byte counts, raw provider response, parsed result, checkpoint and current semantic validation. Changed evidence or provider contracts reject reuse before another request. Original paid files are not edited. This allowed four, then eight completed chunks to be retained across local fixes; only missing/failed chunks incurred new requests.

Final run: `7e350062d8a6784513b5b637`. Source digest: `9fd2890aa10b7c388233eee0f7daeca9b60529d220326ef473a40699a0390697`. Nine accepted chunk responses supply the complete plan. Across all runs there were **13 request attempts, 12 provider responses and one overload attempt with unknown usage**. The 12 reported usages total **472,539 input tokens, including 63,098 cached; 7,386 output; 479,925 total**. These totals include failed semantic responses, not just the final run. Unknown usage is not treated as free. Per-round request cap remained two. No further paid request was needed after script generation.

## Native execution and independent comparison

The generated `stata-replay.do` SHA-256 is `C5DD1AEDD47CFAEB8A8CB3A4BA3C0C27AC3059A0259BAA52FB0D51A9A90F5D9F`. It was transferred without text edits to VM Documents, selected through Stata File > Do, and run in a fresh blank default dataset. The original saved source instance was not cleared or overwritten. Native Results showed both analyses, successful save, `use`, all row/column/type/cell assertions, the completion message and `end of do-file`, returning to the normal prompt without an error. The GUI process remained open; no OS exit-code claim is made.

The actual native result is `C:\Users\user\stata-recorder-85c8e9d9-7b89-4130-acb6-8245a1cb4321\reconstructed.dta`. After script completion, the primary agent separately opened **Data > Data Editor > Data Editor (Browse)** and observed `var1=[3,6,10,14]`, `var2=[5,10,14,22]`, two `float` variables with `%9.0g` format, four observations and the expected order. This was compared with the independent source truth collected before API analysis, not merely the script assertions.

Native summary means/SD/min/max matched source values. Native regression matched the source displayed slope 1.487273, intercept .48, R² .9827, adjusted R² .9741, root MSE 1.1568, N=4, F=113.64, p=.0087, slope SE .1395151 and intercept SE 1.288156. Displayed rounding is the comparison precision; no stronger numerical accuracy is claimed.

Local evidence lives in the ignored recording folder's `native-verification/` (`native-results.jpg` and `browse-reopened.jpg`). Source reference includes `screenshots/evt-00000082-settled-after.jpg` and the separate source-truth JSON. Replay evidence was not uploaded to the API. The analyzer's original `status.json` and the script's text deliberately still say native/source verification was not performed **by the analyzer/script itself**; this document records the separate actual GUI validation and does not rewrite that provenance.

## Limits

This passes one two-column/four-row numeric sample with default summary and one-predictor regression. The UI Map remains a partial 7-section/259-control scan. Other menus, Do-file Editor Tools execution, strings/missing values, imports, multi-frame state, weighted/subset/complex analyses, graphs and edited-after-analysis workflows are not covered by this acceptance. Directory-conflict and nonempty-session guards have offline coverage, not native negative-path validation. No claim of official license integrity or universal Stata support is made.

No raw recordings, screenshots, datasets, request bodies or keys were committed or pushed in this validation turn.
