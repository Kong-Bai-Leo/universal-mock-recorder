# OrCAD / ANSYS 适配进度与验证边界

日期：2026-09-16。目标环境：VM2（34.63.55.195），OrCAD X Capture 与 ANSYS Mechanical / HFSS。用户授权隔离样本、真实 API 分析和结果对照，允许子智能体协作。Code 5.3 在当前子任务模型列表中不可用，使用用户第二选择 GPT-5.6-Sol；未修改录制分析器的模型配置。

## 实机状态

| 产品 | 已证实 | 未证实 / 阻塞 |
| --- | --- | --- |
| OrCAD X Capture 24.1 P001 (4234998)，英文 | About 版本核实；可创建、编辑和保存隔离原理图；Tcl 8.6.5；导线和网标的原生操作与数据库读回已测。 | 不代表 CIS、PSpice 或 PCB 功能许可齐全；UI Map 部分扫描；尚无真实录制到模型到回放的端到端通过结果。 |
| Electronics Desktop 2025 R2 / HFSS | AEDT 可进入工作区；在隔离 Project3 中尝试创建 HFSS 设计。 | 应用报 electronics3d_gui / hfss_gui 不在许可池，HFSS 设计创建失败。未更改许可设置或用 Student 绕过；阻塞已按用户要求发给远程协调任务。 |
| Mechanical / Workbench | 尚未完成本轮安装与运行核实。 | AEDT、Twin Builder、ANSYS Inc 文件夹不能证明 Mechanical / Workbench 可用。 |
| PCB Editor / Presto | 先前开始菜单观察到 OrCAD X PCB Presto 24.1。 | 尚未扫描或验证，不能复用 Capture Tcl 冒充 PCB 回放。 |

之前的 Capture 启动诊断和 ANSYS 隐私选择由用户处理后已不再阻塞工作区。代理没有更改系统安全、隐私或许可证设置，也没有修改用户既有工程。

## OrCAD 原生测试

使用本轮新建隔离工程 C:/Users/user/Documents/RecorderCapture_20260916_1037。

- PlaceWire 1.7 3.0 3.7 3.0 与 PlaceNetAlias 1.7 3.0 RECORDER_TEST 均返回 0。返回值不是对象句柄。
- GetPhysicalGranularity 与 GetDocUnitsPerInch 均为 100.0。导线端点读回 (170,300) → (370,300)，与页面英寸参数一致。此比例只对已核实配置成立。
- 网标名称 RECORDER_TEST，位置 (170,300)；网标 owner 与导线的 GetId(status) 均为 21。不同接口 wrapper 的字符串可能不同，不按句柄字符串判定身份。
- 已验证导线迭代器、对象 ID、页面名称与七类带 status 参数的对象计数。原 GUI 页有两条导线，ID 17、21；其余已查类别计数为零。
- Menu "File::Save" 返回 1，原 GUI 页未保存星号消失；原工程尚未完成关闭重开校验。
- 独立数据库测试通过 CreateDesign → GetRootSchematic → NewPage → NewWireScalar 创建一条线；SaveDesign 的 Failed=0 且文件存在。新文件为同一隔离目录下 RecorderDboProbe_1100.dsn，创建前检查不存在。
- File → Open → Design 打开该 DSN，确认非 PSpice 项目类型；设计树出现 RECORDER_ROOT / REPLAY_PAGE。该活动页面数据库读回 WIRE_COUNT=1、WIRE ID=1、START=(170,300)、END=(370,300)。
- 此独立页面 GUI 当前未显示预期导线，页模板/视图尚待核实；不能把数据库数量通过当完整视觉还原通过。同进程打开也不是独立进程重读验证。

这些是回放接口测试，不是采集时读取内部模型，也不冒充 VLM 识别。

### 完整编译输出实机 smoke test

- 在同一隔离目录另建 GUI 工程 `RecorderReplay_20260916_1123`，空白 `PAGE1`。使用 `tests/fixtures/orcad.mjs` 的合成 IR，由 `renderOrcadTcl` 原样编译；没有手改输出几何。
- 通过 Capture Tcl 分三段传入压缩文本，解码后核对 CRLF 版本为 5898 ASCII 字节、CRC32=2702565299，再执行完整输出。早先一次长文本输入发生截字，不能据此判断生成器接口错误；未使用损坏文本作为通过样本。
- 完整执行 `catch` 返回 0，所有页面/校准/对象数量/新 ID/几何/网标断言通过。110% 视图中可见横向导线及 `RECORDER_TEST`。
- 独立读回为 `WIRES {{17 170 300 370 300}} ALIASES {{RECORDER_TEST 17 170 300}}`：坐标、名称、所属导线和数量一致。
- 原样重复执行返回 `catch=1`、`Wire count mismatch`，在添加内容前被拦住；读回仍是上述单条导线和单个网标。
- **只证明这一个合成样本的完整编译脚本与拒绝路径实机通过**。不证明 VLM 识别、真实录制还原、文件加载、保存重开或全部电路操作。

## 代码与知识包

- ui-maps/orcad-capture/24.1/en-US：部分实机菜单/对话框/页面观察，6 个文档接口候选；来源与实测状态分开。多数菜单、图标和嵌套菜单未扫描。
- ui-maps/ansys-hfss/2025R2/en-US：9 个文档 UI 节点、8 个基础接口候选；不代表许可可用或实机已扫描。
- 独立 OrCAD 采集器已构建为 bin/orcad-recorder/OrcadRecorder.exe，默认 UI Automation 关闭，Capture 进程过滤事件，截图仍为虚拟桌面全图，不自动上传。
- **新录制器已部署并运行**：用户回复“继续”确认后，将 54 KB 的 `OrcadRecorder.exe` 复制到 VM2 的隔离测试目录并启动。已完成两份同 Windows 会话真实录制，UI Automation 均关闭；原始文件已复制回本地，尚无真实模型回放通过结果。
- OrCAD IR / JSON schema、证据参数/身份/覆盖校验，以及受控 Tcl 生成器已编码。脚本核对命名空白页、100 单位校准和对象计数，逐步校验新增 ID、端点、网标名称/owner/位置，不自动保存或覆盖工程。整段合成样本生成脚本已实机运行通过，范围见上。
- 首版仅支持不相交的横竖导线及唯一端点网标；不代表完整电路、器件、连通性或仿真回放。
- OrCAD 分析入口默认只准备证据，显式请求才上传。单个小 session 超预算拒绝，不截断证据。已离线测试跨模型/代码指纹仍共享的两次尝试账本、缺帧/暂停/路径越界拒绝及并发锁。
- 上传前对已核验图片建立独立只读快照并再次核对哈希，避免原文件在请求时变化而发送不同内容；正常及失败路径均清理本次快照并释放锁。只读属性不是抵御具有同目录写权限进程的安全边界。
- VM2 未提供 `//tsclient/C`，但之后通过本地 Explorer 精确选择文件并使用 Copy 菜单，远端 Paste 成功；反向文件夹复制也成功。此前失败的根因未确定。未修改重定向、安全或隐私设置；已通知远程协调任务传输阻塞解除。

## 同会话真实采集与 API 前置验证

- 第一份录制 `20260916-182551-7a0035`：新建隔离 GUI 工程 `RecorderSource_20260916_1825` 后开始，绘制单根水平导线并退出绘线模式。共 16 条事件、20 个原图文件，字节去重后 14 张；超过 12 张预算，因此整段在上传前被拒绝，没有从中删图或改写事件。
- 第二份录制 `20260916-183127-2af750`：独立空白 `RecorderShort_20260916_1831` 工程，使用 W、起点点击、终点双击及 Escape，省去第一份中的重复菜单操作。共 10 条原始事件、14 个原图文件，去重后 8 张。只有两条无截图的 mouse_move 不进入模型输入，全部 8 条其余输入和全部原图的映射保留。
- 两份录制均在界面形成持久水平导线；截图可见起点状态栏 X=2.00、Y=3.00，终点 X=4.00、Y=3.00。此为录制证据检查，尚未完成对第二份源工程的独立数据库读回或模型脚本对照。
- 第二份预检查通过，沿用配置模型 `gpt-5.6`，没有降低图片预算或精度约束。请求体约 2.13 MB；第一次真实客户端尝试在连接阶段失败，错误码 `EACCES`，无 HTTP 状态、无服务端 request ID、无 usage。账本按一次不确定尝试保留，不能声称模型已收到图片或费用为零。
- 申请联网权限做第二次尝试被自动审批拒绝：要求用户明确确认“这份录制的截图和事件发送到 OpenAI”。已向用户发出包含录制 ID、8 张全桌面图、事件、目的地和费用范围的确认；没有绕过拒绝、没有执行第二次请求。
- 超预算错误现显示 unique images、input events、payload bytes、base64 bytes 的实际值/上限；仍整段拒绝，不截断证据。补入合成拒绝路径测试。

## 验证与下一步

离线知识包、采集器源契约、IR 与分析链路测试共 **31/31 通过**（本轮五个定向测试文件，包含预算诊断新回归），耗时约 634 ms。模拟响应测试不是模型准确率测试。

下一步：收到具体载荷的上传确认后，最多再做一次 API 尝试；若返回有效计划，由受控编译器生成单个 Tcl，在新的空白页执行并与源录制独立对照。原始录制和请求文件保留本地。

首轮预算：每应用最多 2 次请求，每次最多 12 张图、6000 输出 tokens，关闭自动网络重试。缺证据、不支持的操作或超预算明确拒绝。

以上为第一次上传确认前的检查点，后续更新如下。HFSS 许可未解决前不发模型请求浪费费用。

### 本轮确认后的更新

- 用户明确允许本任务后续 API 分析，并要求 OrCAD 验证通过后再 commit/push，随后继续其他软件。尝试 2 已成功收到模型响应（服务端返回模型 `gpt-5.6-sol`，请求配置仍为 `gpt-5.6`）。实际 usage：7384 input、0 cached input、614 output（其中 208 reasoning）、7998 total tokens。尝试 1 的 EACCES usage 仍未知；没有把请求字节数折算成费用。
- 模型观察到一条持久水平导线，但认为缺少可读页面坐标，因此返回 `complete=false`、零 operations、空 calibration.evidenceIds。旧校验器先把这个不完整结果误报为 calibration missing；完整原响应已留在录制目录，未修改或伪造。
- 最早可确认的证据策略问题：全局 `imageDetail=low` 被用于 2560×1440 全桌面小字号坐标。OrCAD 现在至少请求 high（原配置为 original 时保留）；不改模型、不删原图。提示词明确检查状态栏 Scale / X / Y，只有与采点时序、命令阶段和持久后态相符时才用于端点，不把游标坐标当选中对象属性。参考 OpenAI 官方 [Images and vision](https://developers.openai.com/api/docs/guides/images-vision#limitations) 关于小文字的限制。
- 本地契约修复：不完整且零操作的计划允许空校准证据并保留审计；完整计划、有操作计划和最终渲染仍必须满足证据和几何要求。没有手改模型结果为可执行。
- 独立实机源图读回：`SOURCE_WIRES {{17 200 300 400 300}} GRANULARITY 100.0 UNITS 100.0`。即页面英寸 `(2,3)→(4,3)`，与原始截图读数一致。这是验收真值，没有加入 API payload。
- 新增显式 `--max-attempts 2..6`，默认仍 2；prepare/analyze 必须一致，报告和指纹记录预算，旧账本不清零。已准备同份 8 图的 high-detail 请求，拟采用 4 次总尝试上限。
- 自动审批拒绝了第 3 次请求，理由为需要用户明确把先前 2 次限额扩到 4 次。该请求**未执行**；已发出具体确认框，确认前不重试、不换样本或通道绕过。
- 录制目录与 UI 截图目录已加 Git 忽略规则。共享 Recorder.cs 中其他应用的未提交改动保留；提交前单独检查 OrCAD 必要依赖，不能直接 add 全部工作树。

当前计数：实际客户端尝试 **2**，成功服务端响应 **1**，可执行模型脚本 **0**，真实端到端通过 **0**；合成编译脚本实机通过的历史范围不变。尚未提交、推送，也未转去其他软件。

本轮修复后的六个定向测试文件合计 **46/46 通过**（含 10 项本地模拟 HTTP 客户端测试，未额外请求 OpenAI），约 1.45 s。另在忽略的 `.equile-local/orcad-precommit` 中构建 OrCAD-only 共享采集器候选：OrCAD 与原 3ds Max 两种条件编译均成功；没有覆盖原工作树或启动该候选程序，尚未暂存。候选用于排除其他应用尚未提交的改动，不能替代真实模型回放验收。

### 恢复工作后的 ANSYS 环境补查

- 在 VM2 的文件资源管理器中，确认 `C:/Program Files/ANSYS Inc/v252` 含 `aisol`、`ansys`、`Framework` 等组件目录；不能再笼统记为只发现 EM 组件，仍不据目录判断功能可用。
- 实际启动已安装的 `Framework/bin/Win64/RunWB2.exe`（不是旁边的批处理文件），启动页显示 Ansys 2025 R2，随后出现空白 Workbench 工作区。
- Workbench 弹窗明确报告无法连接有效的 licensing server；工具箱显示没有适用条目。此为启动/许可检查，不是 Mechanical 建模、脚本或求解通过。
- 未修改许可服务器配置、安全/隐私设置、系统时间或用户原工程；HFSS 先前的许可池错误也未解决。已按用户约定将新阻塞证据发给远程协调任务，请其只读确认可用的合法授权环境，避免双方同时操作 VM2。
- OrCAD 仍沿用原测试录制及两次尝试账本；恢复工作时再次请求用户明确将最多 2 次扩大到 4 次，未在确认前发出第三次请求。

### 用户扩额确认后：第 3 次真实分析

- 用户明确回复“可以”，允许同一录制的 8 张截图和事件再发送至 OpenAI，最多增加 2 次，总上限 4；随后执行第 3 次，没有自动重试。
- 第 3 次真实响应 `complete=true`，输出一条 `PlaceWire(2,3,4,3)`，与此前独立源工程读回一致；未向模型提供源工程对象数据。没有网标，没有 unresolved/pending；末态选中已创建的 `wire-1`。
- 本地最终校验错误地将所有非空选择一律当作未完成/歧义，报 `pending or ambiguous selection at final state`；原始响应保留。正在修复通用末态选择校验，计划离线重新编译同一响应，不为此重新调用 API。
- 第 3 次 usage：28260 input、0 cached、851 output（其中 207 reasoning）、29111 total，返回模型 `gpt-5.6-sol`。历史账本仍保留 1、2、3 次；第 4 次尚未执行。
- 已另建隔离工程 `RecorderModelReplay_20260917_0401`，读取确认 `BLANK_WIRES {}`、`SOURCE_UNCHANGED 1`；此时尚未运行模型编译产物，不能标为真实回放通过。

### 真实导线样本完成回放（VM 时钟 2026-09-17）

- 通用末态选择校验已修复，原始响应未改；通过新增离线 `--compile-existing` 入口核对原请求/响应/图片哈希、账本及状态后，输出新的 Tcl 和来源审计。原失败状态、原响应和使用量保持不变，未调用第 4 次 API。
- 原样 Tcl 文件通过本地/VM 字节数与 CRC32 核对，在隔离 PAGE1 中 `source` 返回 `catch=0`。独立读回 `SOURCE {{17 200 300 400 300}} MODEL {{17 200 300 400 300}} GEOMETRY_MATCH 1 ALIASES {} SOURCE_UNCHANGED 1`。
- GUI 保存、关闭该项目、从最近项目重新打开；激活新原理图后读回仍为 `{{17 200 300 400 300}}`，网标为空。再次执行被 `Wire count mismatch` 拦住，读回不变。
- **通过的是单条独立导线的真实录制→API→编译→原生回放→保存重开对照，不是复杂电路或全功能完成。** 采集 UI 和分析 CLI 仍分开。完整记录见 `orcad-verification-2026-09-17.md`。
- ANSYS 的 HFSS 和 Workbench 许可阻塞仍未解决；没有把文档接口候选或安装目录当作运行验证成功。
