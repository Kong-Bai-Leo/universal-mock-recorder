# JMP 验证记录（进行中）

时间：2026-09-11 开始，更新至 2026-09-12。目标 JMP Trial 19.1.5，VM3 英文界面，2560×1440。

## 2026-09-12 默认单文件 JSL 交付更新

按用户后续“JMP 也是这样的”要求，完整分析现在默认交付 `jmp-replay.jsl`，不要求再把 JSON 给执行 AI。内部 2.0 模型接口契约、提示、schema 与证据校验不变，计划放入 `_internal/replay-plan.json`。受控 JSL 重建最终表格及受支持的分析，不宣称逐项重现历史调用顺序。

- 普通分析与离线重编译均输出唯一 JSL，`primaryOutput` / `replayScript` / 兼容 `validationScript` 指向同一文件；旧导出参数不会多生成一份。准备、预算暂停与真实遗漏不会发布假完整脚本。录制器新增“打开脚本位置”，仅定位文件、不自动运行 JMP；生成和未执行状态明确区分。
- JMP 55 项 + Vivado 30 项，共 **85 项离线回归通过**。覆盖单文件内嵌数据/分析、无原录制/JSON/外部程序运行依赖、隔离输出与拒绝覆盖、默认交付、旧参数兼容、零 API 重编译、分段预算和未完整结果拒绝等。模拟响应不算真实 VLM 准确率。
- 本机 JMP 与 Vivado 录制器重新编译成功；未启动 GUI、未操作 JMP、未更新 VM 包、未提交/push。
- 经 PowerShell 包装入口复用录制 `20260912-084653-a8b952`、原分析 `bc003d14b2a2e625c8bbd60f` 的三段原始 2.0 响应，刻意传入不存在的配置路径以确认离线不读取配置或密钥。0 次 API，原响应及历史状态保留，产物另存 `generated-jmp/offline-compile-IanB2X/jmp-replay.jsl`，3375 字节，SHA256 `d12e74ddc7d9b03bacd9b16f3e70dafd460e20361f185dc73e19d07bd6060882`。
- 新 JSL 的可执行正文与此前实机执行的 `offline-compile-44Wbu1/jmp-replay.jsl` 一致：比较仅排除注释行和编译器生成的输出目录 UUID，没有忽略业务值、接口或断言。此次复用的是新版模型原响应，不将旧 1.0 响应改写成 2.0。新文件仍标为 `saved_replay_script_generated_not_executed`；正文一致性不是本轮重新实机运行或统计对照。

## 2026-09-12 新版接口计划实机验证

用户要求“在 JMP 里验证”后，已在 VM3 JMP Trial 19.1.5 执行新版 `replay-plan.json`。本节是新的“模型选接口 → 执行方按接口复现”验证，区别于下方旧语义 IR / 固定 JSL 历史。

- 录制 `20260912-084653-a8b952`，分析 `bc003d14b2a2e625c8bbd60f`。本地有界验证适配器读取原始 `apiCall` 的接口、对象和参数；未使用旧最终状态编译器，也未改模型业务数据。9 项计划操作验证完成，使用 5 类接口；初始 New Column 依交付契约合入 New Table 构造并验证列绑定，不宣称是 9 次独立原生调用。
- 一次原生执行到达 `JMP API verification 33987cad` 的 PASS 窗口。新表保存重开后：3 行1列，Column 1 / Numeric / Continuous，值 [3,5,8]；保存的 `API_report-1` 重新运行成功。
- 新 Distribution 报告可见 N=3、N Missing=0、Mean=5.3333333、Std Dev=2.5166115、Std Err Mean=1.4529663、Upper95Mean=11.584943、Lower95Mean=-0.918276、最小3/中位5/最大8，与首次分析前独立保存的源真值一致（统计绝对容差 1e-5）。数据为自动断言和界面复核，报告统计是界面读取后独立比较，不是自动统计提取器。
- VM 原生结果：`C:\Users\user\Documents\JMPApiPlanVerification\api-plan-bc099f84-e5f7-49d7-bed6-ae7033987cad\Untitled 3.jmp`，同目录有调用追踪、状态和报告 XML。本机记录在该录制 `validation/api-plan-bc003d14/verification-result.json` 与 `verification.md`；原生文件未拉回本机，未导出结果图。
- 新计划 SHA256 `9863eda47916d6e43d06da8cbeaecf52e0b6087dbad80610000448013d43d192`；执行脚本 SHA256 `9d6edcf19edc51acb8cf6f09f99450f4f0b7e54227f62240b84567a1dee272be`，与实际粘贴文本一致。原始计划和准备阶段 provenance 保留，不回写为事后成功。
- 本次验证新增 API 请求为 0，源表及旧回放保留，未提交/push、未更新 VM 安装包。仅本份建表/写值/Distribution 样本通过；本地验证适配器不等于已实现生产通用执行器，不代表完整 JMP 覆盖。

## 契约变更历史：AI 返回接口调用 JSON

2026-09-12 按用户更正，将后续分析升级为 2.0：AI 必须同时输出操作及明确 `apiCall`（接口/方法、调用类型、接收对象、结果绑定、参数）。接口目录进入模型输入与检查点身份；本地只验证，不从语义操作自动补出调用。默认交付 `replay-plan.json`，不再自动导出 JSL。详见 [契约与示例](jmp-api-plan.md)。

- JMP 离线测试 **45 项通过**；全仓库显式 `tests/*.test.mjs` **256 项通过**。包含缺少模型调用、虚构接口、错误对象绑定、参数冲突、X/Y 反转、旧契约拒绝、跨段续接、JSON 交付和可选验证脚本。测试使用本地模拟响应，不计为真实 VLM 识别。
- 本机 `bin/jmp-recorder/JmpRecorder.exe` 已重新编译；VM3 现有包没有更新，不能宣称 VM 已使用新版接口契约。
- 本次 **0 次付费 API、未操作 JMP 场景、未提交/push**。旧录制、原 provider/parsed 响应与回放结果原样保留；旧 1.0 响应没有模型选择的接口，不能冒充新版结果。
- 契约初次变更时，下方原生执行/源录制对照仅属于旧的“语义 IR → 固定 JSL”链路，新版尚未做实机端到端验证；后来完成的新计划样本验证单列在本文顶部，不能把旧结果冒充新链路证据。

## 旧版语义 IR / JSL 验证历史

- Recorder：已在 VM 内完成两次真实合成数据采集。首版暴露报告出现晚于普通后图的问题；补拍修复后的新样本已确认包含提交后数据及最终 Distribution 报告。
- UI Map：已实机观察 Home / File / Analyze / 新表 / Cols / Column Info、Distribution 启动对话框，以及 Bivariate 报告红三角与 Save Script 子菜单；不是所有功能或图标的完整扫描。
- 离线测试：31 项通过，涵盖严格结构、数值与名称变化、缺失值、非法对象引用、未知初始表、跨段状态、预算及失败重试、延迟截图的保留和跨动作时间归因，以及分段操作编号、已保存响应的离线恢复与证据变更拒绝。测试 API 为本地模拟，费用为零。
- 全仓库离线回归：本轮限定 `tests/*.test.mjs`，242 项通过（此前为239项）。测试包采用显式文件白名单，不包含源数据/录制/.env/用户配置。
- 固定编译器合成 JSL：已在 VM3 JMP 实际运行。首次暴露 New Table 隐式 Column 1 导致多列；修复编译器为构造时显式定义全部列，并新增回归测试（JMP 27 项通过）。
- 修复后合成基础样本：Dose=[1,2,3,4]，Response=[3,5,7,9]。行数4、列数2、两列类型和全部单元格均通过保存重开断言；表内 Distribution 和 Bivariate/Fit Line 脚本重新运行。界面读到均值6、样本标准差2.5819889、N=4、拟合截距1/斜率2、R²=1，与独立算术期望一致。这是编译器合成测试，不是录制识别。
- 基础样本原生输出在 VM3：`C:\Users\user\Documents\JMPRecorderReplays\4629a9ac-8026-4916-a002-9a88d8fa9e9c\JmpSmoke.jmp`。原失败目录 `8254d637-2aa4-4251-ac1f-28013054e84d` 保留错误状态，没有冒充成功。
- 修复后变体样本：ChangedTrial，Dose=[2,5,8]，Response=[11,17,23]。保存重开断言通过；重开的报告显示均值17、样本标准差6、N=3、拟合截距7/斜率2、R²=1、总平方和72，与独立算术期望一致。原生输出为 VM3 `C:\Users\user\Documents\JMPRecorderReplays\461503d0-ae8f-4533-aada-1b2247257ea6\ChangedTrial.jmp`。
- 上述两份修复后脚本由同一固定编译器生成，全文载入 JMP 脚本编辑器执行，未手改业务值。原生数据验证使用自动断言，报告统计值通过 Computer Use 读取并对照；未建立完整的报告统计自动比较器。
- VLM 端到端：本次小型合成录制通过。共 5 次尝试，前 2 次连接失败、后 3 次真实模型返回。最后一段曾因本地操作编号冲突合并失败；修复通用跨段编号逻辑后离线复用原响应生成 JSL，并在 VM3 实际执行、保存、重开和对照。没有追加第 6 次请求。
- 录制前后真实结果一致性：本次 3 行1列 + Distribution 样本通过，数据与报告显示统计值符合首次 API 前保存的独立真值。不是任意 JMP 功能或录制的成功率证明，详见末节。
- 更新版已按用户的“可以”确认启动并重新录制；没有复制密钥到 VM。付费分析在本机运行，不在 VM 保存密钥。
- 最新无密钥测试包已通过 Explorer 文件复制传入 VM3：`C:\Users\user\Documents\jmp-vm-1789197782184`。入口为其 `bin\jmp-recorder\JmpRecorder.exe`。较早的 `jmp-vm-1789193590836` 未含补拍修复；`jmp-vm-1789192418282` 的初版建表生成器还有上述缺陷，均保留但不应作为最新包使用。
- 测试命令审计：一次未限定目录的 `node --test` 误发现既有 `scripts/test-3dsmax-bevel.mjs` 的隔离后台测试，随即中断；只读检查未发现其专属临时路径对应的遗留进程。未加载用户场景或调用 API，该次不计入 JMP 验收。正式回归使用仓库规定的 `tests/*.test.mjs`。

计划样本：小型两列数值表 + Distribution + Bivariate/Fit Line；改表名/行数/数值变体；取消输入/不可读数据反例。源真值独立保存且不进入分析请求。JSL 检查自身 IR 只能证明编译/保存一致，不能代替源录制对照。

## 2026-09-12 首次真实采集发现

- 录制 `20260912-065626-2cf7eb`：在 VM 内新建一列小型合成数值表，并启动 Distribution。36 条事件、36 个引用截图文件、30 个不同内容的图片；去重后的跨段上传计划合计 31 个图片槽、3 段。只做了离线准备，没有上传。
- VM 操作结束后，表与报告已出现正确内容；但录制最后一张 `evt-00000036-after.jpg` 在确认后约 230 ms 拍摄，报告尚未出现。较早的单元格提交后图也出现重绘延迟。不能把确认按钮点击等同于已记录最终报告。
- 通过 Computer Use 输入文本实际使用剪贴板粘贴，因此事件中的 Ctrl+V 不含粘贴的数值。采集器不读取剪贴板，分析必须依赖可读的表格/字段截图，不能把控制字符解释成数据。
- 修复只在 JMP 编译路径增加 `screenshotSettledAfter`：普通后图仍保留；队列空闲时等待 800 ms，若新增输入、暂停、停止或离开目标应用则不补拍。字段名不代表已经经过 CV 稳定性检测，也不保证慢报告必定完成。
- 分析准备保留延迟图的真实时间、原图映射和跨动作归因；延迟图若晚于下一动作，会明确标为可能含后续输入。模型提示要求检查持久表格/报告状态，不按延迟时长判断完成。
- 此修复的本地测试与编译已通过，修复后真实采集也已复测（见下节）；后续本次小样本已完成真实 VLM 与回放对照（见末节）。源数据/统计真值单独留在录制目录的 `validation` 内，不进入模型请求或发布包。
- 更新版无密钥测试包在开发机 `dist/jmp-vm-1789197782184`，含独立 Node 运行时和官方许可证，已传入 VM 并启动。录制器 SHA256 为 `A2BC6DD37FB60922BD75396BEB6E96435FAB3BDF3F55DCBD6273B45B3DD65800`。较早打包尝试因受限网络下载许可证失败而中止，不是 API 分析失败。

## 2026-09-12 补拍复测与真实 API 尝试

- 新录制 `20260912-084653-a8b952`：从无打开数据表的 JMP Home 新建 Untitled 3；Column 1 输入 3、5、8，再执行 Distribution。26 条事件，数字使用实际按键 D3/D5/D8 与 ENTER，不是剪贴板粘贴。
- 38 个引用截图文件，内容去重为 27 张；3 段分别为 10/12/5 张图片、10/11/5 条事件，无证据文件缺失。`evt-00000014-settled-after.jpg` 能看到全部已提交单元格；`evt-00000026-settled-after.jpg` 在最终确认后 1136 ms 捕获到完整报告。本次验证了补拍收益，不代表 800 ms 等待对任意慢报告都足够。
- 独立源真值在首次请求前保存：3 行1列、值 [3,5,8]、N=3、N Missing=0、均值 5.3333333、样本标准差 2.5166115、最小3/中位5/最大8。真值文件不进入模型。源表在录制结束后另存于 VM3 `C:\Users\user\Documents\JmpSource-084653-Preserved.jmp`，文件名改变不属于被录制流程；原始录制表名仍为 Untitled 3。
- 本次单独分析配置保留 gpt-5.6 / low reasoning / low verbosity，只将图像细节设为 high；不修改全局配置或模型。理由是整屏中的单元格/统计文字很小。[OpenAI 图像文档](https://developers.openai.com/api/docs/guides/images-vision) 也提示小字可读性风险；高清设置并不能保证识别正确。
- 首次尝试：本机网络权限 EACCES，无 HTTP 状态、无模型响应。第二次在获准网络权限后连接出现 `ERR_SSL_SSL/TLS_ALERT_BAD_RECORD_MAC`，仍无 HTTP 状态或模型响应。两次均被保守计入原 4 次尝试预算，没有自动重试。
- 不带密钥/录制内容的 HEAD、GET 和约 1.8 MB 合成 POST 探测能在默认 TLS 1.3 / 指定 TLS 1.2 下收到 OpenAI 根地址回应。这只证明基础连接可用，不证明带图片的 Responses 请求已稳定，也不能据此确认是 OpenAI 服务器故障。未关闭证书验证、未改系统安全设置。
- 用户要求将录制拉到本机调用 API 后，核实此前已在本机执行；继续使用同一份本地录制和分析器，只对本次 Node 进程指定 `--tls-max-v1.2`。没有改模型、提示、截图、检查点或系统设置，也没有关闭证书验证。新增 2 次请求都成功；这说明本次传输可用，不能证明原 TLS 故障已普遍修复或确定责任在服务器。
- 两次续接后的阶段状态为 2/3 段：第一段创建 Untitled 3 / Column 1（Numeric、Continuous）并写入 3；第二段写入 5、8 并保留 Distribution 启动对话框的跨段状态。当时只通过数据 IR 对照，尚无报告 operation。后续最终状态见末节。未把真值文件或源 `.jmp` 输入模型。
- 成功请求实际返回模型名 `gpt-5.6-sol`，请求配置仍为 `gpt-5.6`。两次合计 input 77,828 tokens（其中 cached 1,814）、output 1,658 tokens（其中 reasoning 371），total 79,486 tokens。此前 2 次失败没有 usage；费用应与服务方账单核对，不能仅按成功用量宣称全部成本。
- 4 次总尝试用完时进入 `budget_paused`，没有自动重试。用户随后明确同意增加 1 次，续接最后 5 条事件/5 张图，没有重传前两段整包。

## 2026-09-12 最后一段、通用合并修复与实机对照

- 第 5 次尝试成功返回 Distribution：将 Column 1 分配到 Y，确认 OK，最终存在一张表及一个报告。模型给该段 operation 使用 `op-1`，与第一段的局部操作编号重名；原校验器错误地要求响应间的操作标签全局唯一。表/列/报告身份没有冲突。
- 通用修复：合并时把 operation.id 规范化为分段号与段内顺序；该 IR 不通过 operation.id 引用对象。表、列、报告的语义 ID 原样保留，同段重复/非法操作编号与重复语义 ID 仍拒绝。没有修改任何模型数值、选择、统计角色、持久操作或某次 JSL。
- 新增显式离线重新编译模式，核对原 provider 已完成返回与 parsed 一致、逐段请求证据与当前录制一致，并保留来源哈希。此样本三份原响应未经修改，通过新校验与固定编译器；恢复过程 0 次 API。原失败状态保留作历史审计，恢复产物另存 `generated-jmp/offline-compile-44Wbu1/`。
- 生成程序共 9 项操作，26 条事件覆盖，完整建表/数据及 Distribution 角色与独立真值一致；没有通过读取源 `.jmp` 填补视觉信息。
- 生成的 `jmp-replay.jsl` 全文经 Computer Use 载入 VM3 的新 JMP 脚本编辑器并执行，未手改内容。使用新独立目录，保留原源表；输出 VM3 `C:\Users\user\Documents\JMPRecorderReplays\6e63adaa-b03b-4694-a852-1b66e219ac63\Untitled 3.jmp`。
- JMP 到达末尾验证窗口，数据断言通过；保存重开后 Untitled 3 的行数3、列数1、Column 1 名称与 Numeric/Continuous 类型、值[3,5,8]通过编译器断言。随后界面再次确认表格值[3,5,8]、保存的 `Recorder_report-1` 分析脚本，以及该脚本重新打开的 Distribution 报告。
- 独立统计对照：报告显示 N=3、N Missing=0、Mean=5.3333333、Std Dev=2.5166115、Std Err Mean=1.4529663、Upper95Mean=11.584943、Lower95Mean=-0.918276、最小3/中位5/最大8，均与首次 API 前保存的源真值一致（预设绝对容差 1e-5）。数据检查为自动断言，报告统计值由界面读取后独立对照，不冒充报告统计自动提取器。
- 3 次成功请求合计 input 98,104 tokens（cached 3,628 包含在内）、output 2,104（reasoning 438 包含在内），total 100,208；另有 2 次早期失败无 usage，不能当作零费用。本轮仅请求最后一段 1 次。
- 验证边界：本次小型一列 Distribution 样本端到端通过；未验证视频输入、UIA关闭、复杂导入/公式/多因素统计或 JMP Pro，未要求窗口布局像素一致。VM3 现有安装包尚未替换为本轮分析器代码；本轮分析与编译使用开发机修复后的代码，VM只执行生成JSL。未提交/push。
