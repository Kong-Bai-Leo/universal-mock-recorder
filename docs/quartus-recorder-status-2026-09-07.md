# Quartus 录制与还原：2026-09-07 状态

**最新验证（2026-09-08）：** 完整与门录制 `qrec_and_0121` 已经通过生成的 GUI Tcl 在远程 Quartus 的独立新目录实际重建并完整编译；19 项工程、源代码及报告检查通过。详情见下方“完整录制的远程还原验收”。早期章节保留当时状态，不代表最新结论。

目标软件：远程 Windows 内的 Quartus Prime **26.1.1 Build 130 08/06/2026 SC Pro Edition**，英文界面。

路线保持为：截图和鼠标/键盘证据 → 外部模型 API 结合 UI Map 分析 → 结构化 JSON → 本地受控 Tcl → Quartus 官方接口创建原生工程 → 检查工程与结果。采集不调用内部工程接口；构建使用官方接口，是不同阶段。

## 分层验收

| 层级 | 本次状态 | 能证明及限制 |
| --- | --- | --- |
| UI Map | 18 分区、287 节点；271 现场可见、16 仅文档；24 图、14 官方来源；建立阶段 17 测试通过 | 知识条目和来源可追溯；不是所有业务操作均被测试 |
| 录制器配置与编译 | Quartus 配置反射检查通过；Quartus、PSCAD、AutoCAD、KiCad、3ds Max 五个配置编译通过 | 确认进程过滤、manifest、视觉模式与键盘取证规则；不能替代真实录制 |
| API 分析代码／模拟返回 | 最新分析测试 26 项通过 | 验证证据选择、预算、状态继承、本地校验和失败处理；不代表真实模型准确率 |
| JSON／构建后端 | 最新后端测试 37 项通过 | 验证受控代码生成、范围拒绝、路径保护与测试运行时行为；不等于真实 Quartus 执行 |
| 相关回归批次 | 最新相关批次 102/102 通过 | 批次包括所选相关检查，不能与上面定向测试相加 |
| 扩大回归批次 | 121/122 通过；1 项既有 PSCAD 模板 PLAN 测试不匹配 | 未宣称整个仓库测试全通过；未通过手改生成结果掩盖失败 |
| 远程真实采集 | 短录制证据回传复核通过：59 条事件、9 条输入、5 个动作、2 段；55 张引用图存在，选用 19 张；输入 `capture_cancel_probe` 后取消向导 | 输入归属、窗口句柄、截图引用与本地准备均通过；未选中间状态图仍留本地，不声称该短流程代表完整建模覆盖 |
| 真实 API 分析 | 取消向导样本已实测：2 段、19 张不同证据图、4 次成功响应（每段含 1 次校验修复）；9/9 样本核对通过 | 识别项目名 `capture_cancel_probe`、取消与跨段待定操作的取消结论，9 条输入均被表达；没有生成不存在的工程。不是完整工程还原或通用准确率验收 |
| 真实 Quartus 原生构建 | GUI 实际执行 v2 构建脚本成功，消息编号 19686；最终原生文件与编译报告 16/16 检查通过，状态 `native_project_and_compilation_verified`；7 项回读检查通过，HDL 内容与输入完全一致 | 是合成 JSON 构建样本的真实软件执行；不等于取消操作短录制经过 API 的完整还原 |
| 真实完整编译 | 远程 Quartus GUI 消息 21793 显示成功：0 错误、13 警告，耗时 3 分 38 秒 | 是真实安装软件对独立构建样本的编译结果；不是模拟运行时结论 |
| 烧录产物 | 未生成 | 样本未指定引脚及 IO 标准，不具备本次硬件烧录验收结论 |
| 时序与完整还原 | 时序无约束；逻辑等价、真实录制经 API 的完整还原未验收 | 编译成功不能代替时序收敛、逻辑等价与录制对照 |

本记录中的测试数字对应各自批次，不是统一总测试数。API 分析入口、受控构建器和真实软件运行状态必须分别看待。

核心原生工程文件和编译报告已单独收齐，并与生成时的输入逐项核对。QDB 缓存回传仍不完整，不能把回传副本称为完整工程数据库备份；这不影响已经完成的核心文件、HDL、回读结果与编译报告验证。

本地证据：`.equile-local/quartus-development-2026-09-07/native-verification.json` 与 `capture-verification.json`。原生验证记录 `assignmentChecks=7`、`assignmentsVerified=true`、`sourceContentsVerified=true`。创建脚本产生的 readback 保留 `compileCompleted=false`，因为完整编译是随后在 GUI 中独立执行；编译成功依据真实 flow/fitter 报告，不手工修改原回读声明。

真实 API 结果保存在同目录 `live-api-verification.json` 和 `live-api-1788829614551/`。模型返回标识为 `gpt-5.6-sol`；服务返回合计输入 135406 token（含缓存 4014）、输出 2162 token，总计 137568 token，不能把这些数字等同于 Codex 订阅额度或实际美元费用。两段初次返回都把周期观察事件 ID 放入操作输入列表，经过各一次修复后通过本地校验。最终 `complete=false`、`build.status=blocked`，与“向导取消，无可重建工程”的样本事实一致；保留原始模型返回和警告，没有手改工作流。完整创建工程、API 识别后在新目录复建及结果对照仍未验收。现有 verified.zip 是本次 API 验证前的代码包，内含说明是当时状态；本页与本地 API 验证记录提供后续结果，未把密钥或真实证据加入 ZIP。

## 完整创建样本的后续验证（2026-09-08 UTC）

新增独立 GUI 样本 `qrec_and_0121`，录制覆盖新建工程、选择 `A5ED013BB23AE6S`、输入完整与门 Verilog、保存并加入工程。原始证据为 249 条事件、235 张截图，整理为 19 个动作、7 段，预检选出 70 张截图。工程字段、最终器件摘要、全文代码和保存前后证据都在候选中。158 张区间内省略定时图中，88 张与候选文件完全相同，其余 70 张的应用区域像素与候选一致，区别仅在底部任务栏；这个样本未发现省略的不同设计状态，不能推广成通用采样保证。

完整样本的真实 API 调用首次被自动审批在进程启动前拒绝：原图范围为整个虚拟桌面，可能包含其他桌面内容。用户随后明确回复“允许”，授权本次 70 张候选图及相关输入发送至官方 OpenAI API，最多 8 次请求；原命令获准后执行，没有通过其他入口绕过拒绝。

本轮真实分析在第 2 段失败，共 3 次请求：第 1 段通过，第 2 段初次返回及一次修正均把本段过渡观察事件 `evt-00000137`、`evt-00000140` 放进操作的输入事件列表。本段实际输入为 `evt-00000135`、`evt-00000136`。提示要求该操作字段仅引用输入，但请求顶层事件列表混合了输入与观察，通用 schema 校验接受这些观察 ID，后续输入覆盖校验则拒绝并误报“当前操作引用了其他分段的输入”。它们实际属于本段观察，修正反馈也未指出具体事件类型，因此不能仅归因于跨段识别错误，更不能通过放松输入覆盖保护来宣称通过。

已停止继续调用并保留原始模型返回和仅完成第 1 段的检查点。服务返回模型标识 `gpt-5.6-sol`；本轮输入 88778 token（含缓存 2676）、输出 1826 token，总计 90604 token。计划候选为 70 张不同图，但本轮只发送第 1、2 段的 22 张不同图，第 2 段 3 张在修正时重复发送，总计 25 次图像出现。没有最终 workflow、构建材料或 AI 重建工程，完整端到端验证未通过。本轮只诊断和记录，未修改生产代码或手改模型结果。

原始 `.qpf`、`.qsf`、`.v` 已通过远程桌面剪贴板复制到本地作为对照，不输入分析模型。复制时 `.qsf` 尚未写入 `VERILOG_FILE`，而录制末尾 GUI 已显示会话中的加入操作；保留此编译前快照，持久化状态需独立核查。预检、截图核对和原工程参考分别保存在本地开发目录下 `full-recording-preflight/`、`full-recording-evidence-review.json`、`full-recording-original-reference.json`。这些完成项不代表真实录制还原已经通过。

随后在 GUI 对原工程执行完整编译，当前工程名与消息核对一致：消息 21793 显示成功，0 错误、13 警告，耗时 3 分 28 秒。消息 25196、25207 明确因缺少引脚位置和 IO 标准而未生成烧录文件；未建立时序收敛结论。记录见 `full-recording-original-compile-observation.json`。这是原工程的编译基准，编译报告尚未复制回本地，不能代替 AI 重建结果的编译及对照。

## 已实现范围

### 事件引用修复（2026-09-08 UTC，本地验证）

针对上述第 2 段失败，分析请求现分别提供 `inputEventIds`、`observationEventIds`、`evidenceEventIds`，顶层 `sourceEventIds` 只含输入。每段响应 schema 将操作输入引用限制为当前输入枚举；本地验证同时区分“本段观察误当输入”和“不属于本段的输入”，修正请求包含操作 ID、非法事件 ID、分类和允许输入列表。观察仍可用于参数证据、前后截图和完成确认，未放松输入覆盖、证据继承和构建保护，也不自动删除模型填错的编号。

本次 Quartus 定向回归 **84/84 通过**，包括新增的混合事件修正成功、持续错误达到上限、旧/伪造输入拒绝和合法观察确认路径。测试使用模拟模型响应，不调用外部 API 或真实 Quartus。源代码和提示的变更会使旧检查点失效；应保留旧分析目录并用新目录运行。用户要求自行重跑，本次修复没有额外 API 请求，也未验证修复后的真实端到端结果。

- **采集**：仅 `quartus.exe` 的输入归属，截图、鼠标位置及动作、键盘输入、周期状态图、事件/截图时间与原图范围映射；窗口身份包含进程和系统窗口句柄。录制器必须位于目标软件的同一 Windows 会话，不能把本机远程桌面窗口当作 Quartus。
- **知识与分析**：按段检索相关 UI Map；模型输出结构化最终状态与操作证据。采用有上限的请求、修复、图片和文本预算，保存检查点；未知或遗漏不自动升级为已完成。
- **结构化数据**：工程身份、HDL 文件、受限工程/引脚分配、基本主时钟、操作阶段与应用/取消/待定状态。后续证据可明确解决待定操作，不能借旧截图为新数值提供依据。
- **构建**：固定本地代码将 JSON 转为 HDL／SDC 与 Tcl。默认只生成材料；命令行显式执行，或使用 GUI 专用材料在 Tools → Tcl Scripts 中运行，才会创建 `.qpf`／`.qsf`。GUI 也可 File → Open 选择 Script Files 后打开 `build.tcl`，再点击编辑器的蓝色羽毛带播放标记按钮。输出目录必须是新目录，不覆盖既有工程。
- **检查**：创建后关闭并重新打开工程，回读支持范围内的设置与源文件分配；显式命令行编译可保留编译报告。读回设置的正确性与 HDL 逻辑等价分开报告。

## 支持边界

### 跨段最终确认修复（2026-09-08 UTC）

用户重试的完整录制已获得全部 7 段真实模型结果，但旧版最终检查只读取各段原始操作状态，忽略后段明确确认，误将新建目录、打开编辑器和保存对话框的 3 处窗口过渡判成缺口。现将原始缺口和后续确认分别记录，使用同进程、后续时间、合法事件与已选原图严格校验，再计算最终状态；不改写历史操作或模型返回。

新增显式 `--reprocess-from` / `-ReprocessFrom` 离线入口。导入完整旧检查点前核对分段、输入、图片哈希及准备证据；目标必须为独立新目录，不覆盖旧结果，不读取密钥、不调用 API，不伪装为原检查点续跑。当前完整样本离线校验 7 段、33 条输入和 70 张选图通过，3 处误判消除；`complete=true`、构建材料已生成、API 请求 0。实际 Quartus 重建、编译及对照仍未执行，不能据此宣布真实录制完整还原通过。

本次定向回归 131 项：130 通过、0 失败、1 因 Windows 禁止创建文件符号链接而跳过；目录 junction 别名拒绝测试通过。真实数据及报告保留在本地 `full-recording-offline-confirmed/`，分发包不包含这些录制与图片。

支持完整内容的独立 Verilog、SystemVerilog、VHDL 文件；明确工程名、revision、顶层实体、器件系列和器件；受支持的 SEED、OPTIMIZATION_MODE、NUM_PARALLEL_PROCESSORS；字面端口的 LOCATION 与 IO_STANDARD；给定周期（ns）的基本主时钟。

不支持任意 Platform Designer 系统/IP、外部 HDL include 或系统任务、任意路径依赖、完整 SDC/仿真平台、硬件烧录或任意既有工程修改。源文件只有文件名而缺内容、关键值估计/未知、存在未解决持久操作时，阻止构建。UI Map 存在某个菜单或文档主题不表示该操作已经进入构建支持范围。

`complete` 是在证据与支持范围检查下的覆盖状态，不是模型理解正确、工程实际创建、编译成功或还原等价的统一合格证。`analysis-status.json` 中 `status=succeeded` 仍可能同时是 `build.status=blocked`。

## 证据与交付

### 完整录制的远程还原验收（2026-09-08）

用户明确要求在远程电脑的 Quartus 复现后，将 `full-recording-gui-confirmed/` 生成材料复制到远程 `C:/Users/user/Documents/full-recording-gui-confirmed/`。在关闭原工程后的空白 Quartus 会话执行 `build.tcl`，软件报告脚本执行成功（消息 19686），实际创建新的 `qrec_and_0121.qpf/.qsf`，关闭再打开并完成 6 项设置与源内容回读。随后打开该新工程执行完整编译，消息 21793 显示 **0 错误、13 警告**，仪表板耗时 **3 分 32 秒**。

通过远程桌面剪贴板回传核心工程文件和 `output_files`，逐项验证 **19/19 通过**，保存了 **16 份原生编译报告**的哈希。回传的 Tcl、workflow JSON 和 HDL 与生成清单哈希一致。工程名、revision、顶层模块、Agilex 5 系列、`A5ED013BB23AE6S` 器件和 Verilog 分配均与录制参考一致；原录制源代码与重建 HDL 仅缩进、换行形式和文件末尾换行不同。Flow、Fitter、Assembler 报告均为 Successful，报告输出目录明确属于独立重建工程。Flow 资源摘要为 1 ALM、0 寄存器、3 引脚。

本次可以确认这个真实录制样本的工程设置和源代码被还原，且已在真实 Quartus 完整编译。原工程的 GUI 编译基准同为 0 错误、13 警告，3 分 28 秒；没有复制原工程编译报告，因此不声称已逐项比对两者布局布线或报告资源。原 QSF 仍保留为编译前快照，其中缺少持久化的 VERILOG_FILE，不修改该历史证据。

缺少引脚位置和 IO 标准的警告 25196、25207 仍存在，未生成烧录文件；未测试硬件运行、布局后形式等价和时序收敛。`quartus-readback.json` 的 `compileCompleted=false` 保持原值，因为创建脚本不负责随后独立触发的 GUI 编译。QDB/DNI 编译缓存未回传，副本不是完整数据库备份。此结论只适用于当前三端口与门样本，不代表所有 Quartus 操作均可还原。

本次还原没有 API 请求，没有手改模型返回或生成脚本。独立核验报告位于本地 `.equile-local/quartus-development-2026-09-07/full-recording-replay-verification.json`，核心原生副本位于 `full-recording-native-replay/`。分发 ZIP 没有重新打包，其内说明仍对应打包时状态。

UI Map 原始截图、真实录制、请求体与密钥留在本地。分发包只带可运行程序、必要分析模块、知识条目及证据元数据，可选 Node 运行时；不得夹带 `config.json`、`.env` 或真实录制。打包后的知识检索不依赖扫描原图，原图完整性复核需在有原始证据的开发目录运行。

使用方法见 `src/Recorder.Quartus/README.md`；地图范围与历史扫描见 `ui-maps/quartus/26.1.1-pro/en-US/README.md`。本记录不授予额外付费调用或用户工程操作权限。
