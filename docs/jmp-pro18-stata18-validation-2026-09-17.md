# JMP Pro 18 / Stata 18 — current run and acceptance gates

This run targets the user's **old VM only**. The new VM has not been operated. Earlier JMP Trial 19.1 results do not count as Pro 18 acceptance.

## Established so far

| Layer | JMP Pro 18 | Stata 18 |
| --- | --- | --- |
| Observed application | Pro 18.0.0 (746439), English | Stata/MP 18.0, English, initially empty |
| UI knowledge | Actual partial scan: 5 sections, 111 controls; Home/File/New, Help/About, table, Column Info/Modeling Type, Analyze and Distribution launcher | Actual partial scan: 7 sections, 259 controls; main panes, File, Data, empty Data Editor, Statistics/summary/linear dialogs, Window > New Do-file Editor, blank editor shell, File/Open submenu and canceled Open dialog, plus seven disabled Tools entries |
| Capture | Dedicated build; version/edition read from executable metadata, language explicitly confirmed, frozen target process/session | Dedicated build; official MP/SE/BE process-name recognition, analysis currently accepts observed MP only |
| Analysis | Version-matched map and interface catalog, explicit model-selected calls, checkpoints and bounded requests | Same layered method with Stata-specific evidence, schema, catalog and operations |
| Single-file output | Controlled JSL renderer; Pro 18 synthetic files compiled offline | Controlled do-file renderer; portable UUID output directory relative to target Stata working directory, no analysis-host path |
| Native real-recording validation | **PASS for one four-row Numeric/Continuous Distribution sample**, including unchanged JSL execution, save/reopen and independent GUI source comparison; see [bounded verification](jmp-pro18-real-recording-verification-2026-09-17.md) | **PASS for one bounded two-column/four-row float sample**: real recording → 9/9 model chunks → unchanged single `.do` executed in an isolated blank Stata/MP 18 instance → save/reopen/type/cell assertions and independent GUI summary/regression comparison. Not a general Stata pass; see [bounded verification](stata18-real-recording-verification-2026-09-18.md) |

JMP's About dialog included a third-party modification marker. No licensing files/settings were changed. Tests describe this particular observed installation, not certified official binary/license integrity.

Historical local joint suite rerun on 2026-09-17: **108/108 passing** with `tests/jmp.test.mjs`, `tests/jmp-coverage.test.mjs`, `tests/jmp-resume.test.mjs`, `tests/jmp-package.test.mjs`, `tests/stata.test.mjs`, `tests/stata-package.test.mjs`, `tests/stata-ui-map.test.mjs`, and `tests/statistical-recorder-capture.test.mjs`. The current Stata-related offline suite has **39 passing tests**; those synthetic checks are separate from the new bounded Stata native result. Stata cases include preserving observed storage types, rejecting X values that become constant after float storage rounding, explicit resolution of cross-chunk pending input, JSON-stable unknown cells, saved-dataset lifecycle and save/reopen type assertions.

The joint eight-file suite was rerun after the Stata fixes on 2026-09-18 VM time: **116/116 tests passed**. These remain offline/synthetic regressions, separate from both applications' native sample checks.

The capture engine now records target loss as a terminal `target_lost` event plus manifest marker; analysis rejects it before upload. Process exit/restart must not silently yield a complete-looking truncated recording. Unknown metadata stays unknown rather than defaulting to a different version.

## Deployment checkpoint

- JMP development package copied with Explorer/RDP file copy into the old VM's Documents folder, retaining the package's unique name. It contains no credentials, user recordings or source tables.
- The package executable matches the latest local capture build by local SHA-256 comparison. The user approved launch; the recorder started in the old VM and completed a real UI capture.
- JMP sample `20260917-151021-b446a0` covers new table creation, numeric/continuous column naming, four cell entries, Distribution role assignment and report creation. Capture contains 67 events and 76 screenshot files; exact-byte deduplication yields 48 unique images and 51 upload slots over five chunks. Process/session binding and Pro 18.0.0 / en-US metadata were verified from the saved manifest; target loss is false.
- The source table was saved separately in VM Documents as `JMP18-source-20260917-151021.jmp`. A local independent truth record was preserved before API submission, including cells and native Distribution statistics. It is verification-only, not VLM input.
- For this validation only, an ignored local config used high image detail so small table/report text was legible. Five paid chunk responses completed under the authorized budget. Earlier returned chunks were safely reused after a replay-scope clarification; the final JSL was transferred unchanged and executed in Pro 18. The first sandboxed EACCES attempt had no confirmed response/usage; its cost is unknown, not zero. The five successful responses report 196,999 input tokens (13,616 cached), 3,827 output tokens and 200,826 total tokens. See the [bounded verification](jmp-pro18-real-recording-verification-2026-09-17.md).
- All raw recordings, screenshots, request payloads and source tables remain local and excluded from commit/push.

## JMP result and remaining scope

The real recording, API interpretation, controlled compile, unchanged Pro 18 JSL execution, saved/reopened table and report, and independent GUI comparison all passed for this one sample. The source truth was captured before API submission and kept out of VLM input. The JSL self-check still says `sourceRecordingCompared=false`, correctly: the primary agent's separate GUI check, not the script or a user-performed check, established the match. Bivariate, Fit Line, changed-data variants, Pro-only analyses and generalization remain untested.

## Stata sequence and bounded outcome

Stata now has a partial 7-section/259-control live map. Tools execution entries were observed disabled in a blank Do-file Editor, not executed. File > Open submenu and the file chooser were inspected and canceled without choosing a script. Toolbar functions and other unexplored descendants remain unverified.

The user subsequently authorized the first recorder launch. The recorder ran successfully on the old VM. Starting from the maximized empty Data Editor, the primary agent entered `var1=[2,5,9,12]` and `var2=[4,8,13,19]` through individual UI cells. The native grid and variable panel visibly confirmed all eight cells, four observations and both storage types as `float`. The recorder displayed 32 events before stop; its stopped status confirmed the recording was saved in the VM package's recordings directory. This is a **data-entry-only capture checkpoint**, not the complete statistical workflow and not a claim that all saved frames have been audited. The VM clock displayed 2026-09-18 during this capture.

The Data Editor covered the Results startup text during capture. No paid Stata API request was made. After the user's explicit confirmation, the original instance's Results display was cleared while recording was stopped; its two variables and four observations remained intact. Those data were then saved through File > Save as to the new VM Documents file `Stata18-data-entry-checkpoint-20260918-0243.dta`. Stata explicitly reported the file saved, and the original instance was closed normally without clearing data or overwriting a file. The stopped recording remains preserved in the VM.

A separate fresh Stata/MP 18.0 instance was opened and visually confirmed to have empty History and a blank default dataset (zero variables/observations). A new frame was considered but not used: the current analysis contract explicitly excludes multiple frames, while the runtime blank guard only sees the active dataset. The capture engine also refuses ambiguous multiple visible target processes, so the saved original instance was closed before starting a new capture. The new instance's startup Results text again contains license identifiers; separate confirmation to clear that new display has been requested and is pending. Recording remains stopped. No sequence after the first stop is being stitched into the original capture or claimed as an end-to-end pass.

Update 2026-09-18 VM time: the user explicitly confirmed clearing the new Results startup display; this was completed before capture. A fresh continuous recording `20260918-034557-105f30` now covers blank data, GUI cell entry (`var1=[3,6,10,14]`, `var2=[5,10,14,22]`, both float), default summary/regression, source save, source reopen and Browse verification. It contains 89 events and 120 source frames. Independent source truth was saved outside VLM inputs. Earlier pending-display statements above describe the prior checkpoint, not the current state. See [current verification gates](stata18-real-recording-verification-2026-09-18.md).

The Windows metadata version `521.18.0.120` initially failed the version gate. Conservative normalization accepts consistent observed `521.18.0.*` product/file metadata, retains the raw manifest and rejects conflicting or other-release metadata. Preparation retained all events/images, deduplicating to 95 unique images over nine chunks. The earlier automatic-review stop was a pre-upload historical gate; the user subsequently authorized the bounded transfer and analysis. Generic fixes—not hand edits to response, recording or script—covered epoch timestamps, fixed receiver/result IDs, observational blank overlap, JSON-stable `null` for unobserved cells, saved-dataset lifecycle, and strict reuse of prior genuine responses after code repairs.

All nine chunks of continuous sample `20260918-034557-105f30` ultimately validated. The generated single `.do` was transferred unchanged and invoked through the main Stata window's File > Do in a separate blank Stata/MP 18 instance. It reached the end, saved/reopened its isolated `.dta`, and passed storage-type and every-cell assertions. GUI summary and regression values matched an independent source truth recorded before model submission and kept out of VLM inputs. The source values were `var1=[3,6,10,14]`, `var2=[5,10,14,22]`, both float, with default summary and `regress var2 var1`. This proves this sample, not all Stata workflows, editions or UI controls. The partial UI Map remains 7 sections/259 controls; Do-file Editor Tools execution remains untested despite the successful File > Do invocation.

There were 13 paid attempts, 12 responses and one overloaded attempt with unknown usage. Provider-reported aggregate usage was 472,539 input tokens (63,098 cached), 7,386 output and 479,925 total; no dollar estimate is asserted. The configured alias `gpt-5.6` was reported as `gpt-5.6-sol` by responses. The generated `.do` embeds neither the analysis host's absolute path nor an implicit `clear`, overwrite, external dataset dependency or arbitrary model-supplied command.

Do not transfer either application's single-sample pass to other operations or installations; each needs its own API/native/source-comparison gates.
