# 课时67 创建块 —— 命令流(S2)

覆盖率:读了 14/14 张蒙太奇(100%),`cv/manifest.json` 记录 scanned=23726 帧 = 全片。

## 进入执行序列的命令

| 时间 | 命令行回显 | 参数 |
|---|---|---|
| 106.6–108.0s | `_circle` 指定圆心 / 指定半径 | 两次 |
| 108.5–113.8s | `_arc` 起点/第二点/端点 | |
| 102.7s | `PLINE` 指定起点,当前线宽为 0.0000 | |
| 99.4s | `_line` 指定第一个点 / 指定下一点 | |
| 126.7s 起(多次) | `B` → `BLOCK 找到 5 个` / `指定插入基点` | **块由 5 个对象组成** |
| 415.2 / 469 / 613.4 / 793.9s | `X` → `EXPLODE 找到 1 个` | 分解块参照 |
| 600.0 / 657.6 / 709.0 / 865.4 / 913.0s | `-INSERT` `单位: 毫米 转换: 1.0000` `_Scale 指定 XYZ 轴的比例因子 <1>: 1` `_Rotate 指定旋转角度 <0>: 0` | **比例 1,旋转 0** |
| 630.7s | `M` → `MOVE 找到 5 个` | |
| 609.6s | `_.erase 找到 5 个` | |
| 935.5–939.4s | `X` → `EXPLODE 找到 1 个` → **`无法分解 1。`** | 演示"不允许分解"的块 |

## 剔除(取消/撤销/重画,不进入执行序列)

- 44.6–48.1s、95.5–96.0s 窗选 `*取消*`
- 100.3–100.8s `LINE` 两次 `*取消*`
- 103.2–103.7s `PLINE` `*取消*`
- 110s 附近 `_arc` `*取消*`
- 116.2–118.1s `MOVE 找到 1 个` `*取消*`
- 119.0–121.9s / 408–413s / 537–539s `COPY 找到 1 个 复制模式=多个` `*取消*`
- 495–498s 夹点 `** 拉伸 **` `*取消*`
- 634.1–635.5s / 892.8–894.7s `H`→`HATCH 拾取内部点` `*取消*`
- 922.1–925.4s `_layiso` `选择要隔离的图层上的对象: 找到 0 个` `*取消*`
- 全片大量 `指定对角点 [栏选/圈围/圈交]: *取消*` 的空窗选

## 对话框读回(命令行不回显的参数)

- t=800s 图层特性管理器:只有 `0` 与 `图层1`,均为 白/Continuous/默认
- t=650s "块"库下拉列表中的块名:**444 / 快1 / 两条线 / 线**
- t=944s 特性面板:`块参照 名称 888,图层 0,旋转 0`

## 几何测量

t=854s,`geo.json`(measure.py,`--color white`)。三个实例的"小圆圆心→大圆弧圆心"偏移
分别为 (64.8,10.8)、(62.4,12.0)、(64.8,12.0),互差 ≤1.5px,证实三者是同一个块的三次插入。
单个实例的 5 个对象(小圆 / 多段线 / 圆弧 / 两条短线)见 `frames/l67_fig1.png` 的 20px 网格图。
