# 课时80 视口与比例

## 命令流

读了 3/20 张蒙太奇(m11、m16 及抽样)。源图文件名回显 `8.03代练`(与课时79 同一套图)。

提取到的命令与选项(证据等级 **C**,均为命令行回显):

| 时刻 | 命令 / 回显 |
|---|---|
| 524.8–529.6s | `CO / COPY 找到 1 个`,`当前设置: 复制模式 = 多个`;选项集 `[位移(D)/模式(O)]`、`[阵列(A)/退出(E)/放弃(U)]` |
| 531.5s | `M / MOVE 找到 3 个` |
| 539.2–541.6s | `M / MOVE 找到 1 个`,`<正交 关>` |
| 761.5–765.8s | `MV / MVIEW`,完整选项集回显:`[开(ON)/关(OFF)/布满(F)/着色打印(S)/锁定(L)/新建(NE)/命名(NA)/对象(O)/多边形(P)/恢复(R)/图层(LA)/2/3/4] <布满>` |
| 766.3 / 808.1 / 840.3 / 843.1s | `_.MSPACE`(进模型空间) |
| 798.0 / 801.9 / 816.3 / 817.7 / 839.8s | `_.PSPACE`(回图纸空间) |
| 778.3 / 812.4 / 816.3 / 817.7 / 839.8s | `-VIEW` 选项集 `[?/删除(D)/正交(O)/恢复(R)/保存(S)/设置(E)/窗口(W)]` → `_RESTORE 输入要恢复的视图名: "佩奇1"` |
| 801.9–808.1s | 夹点 `** 拉伸 **`,选项 `[基点(B)/复制(C)/放弃(U)/退出(X)]` |
| 843.1s | `_u INTELLIPAN`(撤销平移) |

**命名视图名为 `"佩奇1"`** —— 这是本课唯一被命令行明确回显的字符串参数。

## 剔除(视频里的取消/撤销,不进入执行序列)

518.6 / 521.0 / 527.7 / 529.6 / 530.1 / 532.0 / 532.5 / 535.8 / 541.6s 连续 `*取消*`;
766.3 / 808.1 / 840.3 / 843.1s 窗选后 `*取消*`;
843.1s 的 `_u INTELLIPAN` 撤销;
524.8–529.6s 的 COPY 与 531.5–541.6s 的三次 MOVE **全部以取消收尾**,没有产生持久修改。

## 构造

- A3 图幅 420×297 与内框:承接课时77 命令行确认的纸张尺寸(**C**)
- 模型空间图元(圆 + 矩形):布局排列,用来让视口里有可见内容(**L**)
- `TILEMODE 0` 进布局 + 两个 `MVIEW` 视口:对应视频里的 MVIEW(**C** 命令,**L** 位置)

## 验证

`verify.json`:`VIEWPORT×3, LWPOLYLINE×2, CIRCLE×1`
(3 个 VIEWPORT = 2 个 MVIEW 视口 + 布局自身的记录视口)。回放日志无报错。

## 证据等级

- **C**:MVIEW 及其选项集、MSPACE/PSPACE 切换、`-VIEW _RESTORE "佩奇1"`、COPY/MOVE 的选择集计数、A3 图幅尺寸
- **L**:模型空间图元、视口位置与大小

## 做不到的部分

1. **视口比例未设定** —— 这是本课的核心内容,但比例是在**状态栏下拉列表里点选**的,
   命令行零回显,对话框帧也未捕获到选定值。按铁律 R4 不编造,成品含视口但没有比例。
   要补全需要新增一条证据通道:状态栏比例框的定点 OCR。
2. **命名视图 `"佩奇1"` 的视图范围未复现** —— `-VIEW _RESTORE` 只回显名字,
   视图的窗口范围是先前用鼠标框选保存的,无坐标回显。
3. `COPYCLIP`/`PASTECLIP` 在无头环境无剪贴板(SOP 坑表已列),未纳入。
4. 夹点 `** 拉伸 **` 无法在脚本中回放,且视频里全部以取消收尾,未纳入。
