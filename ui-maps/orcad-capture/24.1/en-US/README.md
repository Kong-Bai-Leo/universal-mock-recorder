# OrCAD X Capture 24.1 — partial UI observations and Tcl candidates

On 2026-09-16, Capture's English working UI and About **24.1 P001 (4234998)** were observed in VM2. The isolated GUI project `RecorderCapture_20260916_1037` and a GUI wire were created. In its Command Window, `PlaceWire 1.7 3.0 3.7 3.0` and `PlaceNetAlias 1.7 3.0 RECORDER_TEST` each returned 0 and produced visible objects. `GetActivePage` yielded both `GetPhysicalGranularity=100.0` and `GetDocUnitsPerInch=100.0`; the test wire's internal endpoints read back as `(170,300)` and `(370,300)`. Database iteration on GUI `PAGE1` found two wire IDs (17 and 21), and the test alias's name, `(170,300)` location and owner wire ID 21 were read back. `Menu "File::Save"` returned 1 and the GUI title asterisk disappeared, without a reopen test. This proves neither full licence entitlements nor model-generated replay accuracy. The Start Page displayed a conflicting 23.1 banner; About is the recorded product-version evidence.

`ui-map.json` preserves the observed hierarchy, with Place and New Project children stored separately. `live-observation.json` records the observation scope and approximate rectangles at one 2560×1440 layout. No Automation IDs or reusable click coordinates are fabricated. Only first-level Place entries were transcribed; toolbar icons, nested submenus and most other menus remain unscanned. The screenshots were inspected in the conversation; this package does not claim persistent image files.

`index.json` also links the source registry and three bounded interface groups. These retain their original documentation provenance and conservative unverified interface-contract markers: the local readbacks do not validate all parameters, general units, other object identities or prerequisites. Product-level Cadence material establishes Tcl as an extension mechanism; command forms come from Cadence Community replies or a user example, not a complete official 24.1 API specification. Do not treat these entries as an executable whitelist without calibration and a separately implemented validator/compiler. `capNewProject` opened a dialog, not a proven unattended project constructor. A separate Dbo probe created a DSN and was subsequently opened in the same process; wire count/endpoints read back correctly, but its wire was not visible at that view. No OPJ or Dbo net alias was created by that probe.

The offline test checks JSON references, hierarchy, unique IDs, provenance and bounded runtime markers only. It does not call Capture, the network or a model. The capture-only recorder under `src/Recorder.Orcad` saves events and screenshots; the separate `src/analyzer/orcad-cli.mjs` retrieves this knowledge and compiles validated operations to Tcl.

On 2026-09-17 (VM clock), one real single-wire recording passed API analysis, untouched model-response compilation, Tcl source-file execution, independent endpoint comparison, GUI save/project close/reopen, and nonblank-page rerun refusal. `live-observation.json.realRecordingVerification` records the scope separately from the earlier synthetic fixture. This does not establish complete UI coverage, components, connectivity, simulation, complex workflows or independent-process replay. Capture, PCB Editor, Presto, System Capture and SKILL capabilities must not be conflated.
