# JMP 执行环境

2026-09-11：源码位于笔记本工作区，目标为 VM3（35.192.246.87）的 JMP Trial 19.1.5 英文版，About 已在前一轮实际确认。非 JMP Pro。Windows Server 2022，RDP 画面 2560×1440。

本轮 Computer Use 已看到 Home、File、Analyze、空白数据表；新建表默认包含 Column 1、0 行。RDP 会出现截图延迟，输入后须等待新快照确认，不连续盲点。

采集器必须与 jmp.exe 位于 VM 内同一桌面会话；外层 mstsc.exe 不属于采集目标。采集端不读 JMP 内部表数据/JSL。系统截图会包含遮挡窗口，进程过滤不会自动脱敏。

JmpRecorder.exe 已在本机编译；文件夹复制通道已实测可将测试包传入 VM Documents。`\\tsclient\C` 共享磁盘不可用，不依赖该路径。VM 内采集器尚未启动，等待新构建程序启动确认；没有上传或复制密钥。

UI Map 为首批工作流的部分扫描，不代表软件全覆盖。JSL 后端使用官方脚本能力；基础和改名/数值/行数变体已在 VM 中保存 `.jmp`、关闭重开并验证全部单元格和列属性，报告统计值与合成样本的独立期望一致。详见 `docs/jmp-verification-2026-09-11.md`。这不等于录制/VLM 识别已通过。
