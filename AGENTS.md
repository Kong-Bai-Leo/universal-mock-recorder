# Recorder 开发约定

本项目后续新增或改进任何软件的 recorder，均以已有技能和经验总结为工作基线。这是用户明确要求的长期项目约定，不只适用于 AutoCAD 和 3ds Max。

## 开始工作前

- 新增软件、UI Map、操作支持或调整分析/生成链路：先完整阅读并遵循 [recorder-adapter](skills/recorder-adapter/SKILL.md)。
- 排查录制漏操作、数值错误、生成失败、耗时或成本：先完整阅读并遵循 [recorder-diagnose](skills/recorder-diagnose/SKILL.md)，将其中方法迁移到当前应用；只要求诊断时不直接实施修复。
- 验证修改、设计回归或检查实际还原效果：先完整阅读并遵循 [recorder-verify](skills/recorder-verify/SKILL.md)。
- 开发规划和验收参考 [经验总结与 Agenda](docs/recorder-skills-and-agenda.md)。只读取与当前任务有关的其他资料，不必每次加载全部历史。

技能安装副本与仓库内容不一致时，先核实差异；本项目的工作方法以仓库中经用户确认的版本为维护源。不要静默覆盖个人技能中的其他修改。

## 不变的开发原则

- 修复采集、识别、状态管理和生成逻辑，不靠手改某次 SCR/MS 或硬编码特定作品来宣称问题解决。
- 通用采集、应用 UI/命令知识、结构化操作、回放后端分层；默认不依赖目标软件内部接口采集，可选增强与基础能力明确区分。
- 保留操作前后及必要中间证据、时间和原图映射；先确定命令阶段、选择层级与参数角色，再解释数字和输入。
- 跨段维护合法对象身份、依赖与拓扑变化；不编造实体 ID，不把视觉面 ID 当作运行时面编号。
- 精确、估算、未知与遗漏分别表达；完整输出不代表尺寸精确，脚本能运行不代表结果还原正确。
- 验证采集、分析、编译和实际结果，并分别报告已实现、已测试与未支持的范围。
- 关注请求成本、证据预算、检查点与修复上限；技能本身不授权额外付费上传、操作用户场景或提交/push。
- 录制内容、截图、请求体和密钥默认留本地；保留其他任务的未提交改动。

## 新应用与规则更新

迁移通用方法，不照搬 AutoCAD/3ds Max 的按钮、坐标、命令或特定几何假设。新软件确实需要不同方案时，说明不适用的原规则、依据与替代办法；涉及范围、数据或权限变化时先取得相应授权。

Agenda 是可更新的优先级和验收基线，不是必须先清空的任务队列，不应阻止用户明确指定的新任务。用户后续明确要求优先于这些项目惯例；不要因为旧经验而悄悄改变新需求。
